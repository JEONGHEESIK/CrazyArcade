# 게임 환경 설정
GAME_HOST = "127.0.0.1"
GAME_PORT = 12345

# 맵 크기
BOARD_ROWS = 13
BOARD_COLS = 15
BOARD_SIZE = BOARD_ROWS * BOARD_COLS  # 195

# 상태 공간 크기
STATE_SIZE = 607

# 행동 공간 크기
ACTION_SIZE = 6  # IDLE, UP, DOWN, LEFT, RIGHT, PLACE_BOMB

# ============================================
# DQN 하이퍼파라미터 (개선)
# ============================================
LEARNING_RATE = 0.0001  # 0.0001 → 0.0005 (5배 증가)
GAMMA = 0.99  # 0.95 → 0.99 (장기 보상 중시)
EPSILON_START = 1.0
EPSILON_MIN = 0.1  # 0.05 → 0.01 (더 많이 활용)
EPSILON_DECAY = 0.9995

# 메모리 및 배치
MEMORY_SIZE = 100000  # 50000 → 100000 (더 많은 경험)
BATCH_SIZE = 128  # 128 → 64 (더 자주 업데이트)
TARGET_UPDATE_FREQUENCY = 10  # 5 → 10 (안정성)

# ============================================
# PPO 하이퍼파라미터
# ============================================
PPO_LEARNING_RATE = 0.0005  # 0.001 → 0.0003 (DQN과 비슷하게)
PPO_GAMMA = 0.99
PPO_GAE_LAMBDA = 0.95
PPO_CLIP_EPSILON = 0.3
PPO_VALUE_COEF = 0.5
PPO_ENTROPY_COEF = 0.02  # 0.02 → 0.01 (탐험 약간 감소)
PPO_EPOCHS = 10
PPO_BATCH_SIZE = 64

# 학습 설정
MAX_EPISODES = 1000
MAX_STEPS_PER_EPISODE = 10000
SAVE_FREQUENCY = 50

# 네트워크 구조
HIDDEN_SIZE_1 = 256
HIDDEN_SIZE_2 = 256
HIDDEN_SIZE_3 = 128

# 보상 설정 (동일)
REWARD_COLLECT_ITEM = 20.0
REWARD_COLLECT_BALLON = 30.0
REWARD_COLLECT_POTION = 35.0
REWARD_COLLECT_SKATE = 25.0

REWARD_PLACE_BOMB_NEAR_ENEMY = 15.0
REWARD_HIT_ENEMY = 100.0
REWARD_WIN_GAME = 300.0

REWARD_TRAP_ENEMY = 50.0
REWARD_POP_ENEMY = 200.0
REWARD_AUTO_KO = 50.0
REWARD_APPROACH_TRAPPED = 3.0
REWARD_FAST_POP_BONUS = 50.0

REWARD_IN_DANGER_ZONE = -5.0
REWARD_PLACE_BOMB = -3.0
REWARD_ESCAPE_DANGER = 10.0
REWARD_GET_TRAPPED = -60.0
REWARD_GET_POPPED = -120.0
REWARD_AUTO_DEATH = -90.0
REWARD_DIE = -300.0

REWARD_MOVE_TO_ENEMY = 3.0
REWARD_MOVE_TO_ITEM = 3.0
REWARD_MOVE_AWAY_COWARD = -1.0
REWARD_IDLE_PENALTY = -1.0
REWARD_TIME_PENALTY = -0.01
REWARD_SURVIVE_STEP = 0.0
REWARD_ACTIVE_MOVEMENT = 2.0
REWARD_OPTIMAL_DISTANCE = 1.0
REWARD_TOO_CLOSE_PENALTY = -1.0
REWARD_EARLY_ITEM_BONUS = 15.0
REWARD_IDLE_CHAIN_PENALTY = -3.0

# 로그 및 저장 경로
LOG_DIR = "./logs"
MODEL_DIR = "./models"
TENSORBOARD_DIR = "./runs"
